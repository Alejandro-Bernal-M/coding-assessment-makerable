
<%= render @patient %>

<div class="show-patient-btns">
  <% if current_user.role == 'Receptionist' %>
    <%= button_to "Destroy this patient", @patient, method: :delete, class:'btn' %>
    <%= link_to "Edit this patient", edit_patient_path(@patient), class:'btn' %> 
  <% end %>
    <%= link_to "Back to patients", patients_path, class:'btn' %>
</div>
